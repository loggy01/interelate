[pytest]
minversion = 8.0
testpaths = test
addopts =
    -ra
    --strict-config
    --strict-markers
    --cov=interelate
    --cov-report=term-missing
markers =
    integration: runs multiple package stages together using real local files
    smoke: runs the installed command as a user would run it
